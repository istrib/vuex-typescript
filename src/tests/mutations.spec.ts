import * as lib from "../"; 
import { Product, ProductInBasket, BasketState } from "./basketState"

class BasketMutations {
    constructor()

    addToBasket(product: Product, quantity: number) {

    }
}